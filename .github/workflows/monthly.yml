name: Monthly Econ Light Update

on:
  workflow_dispatch:
  schedule:
    - cron: "20 12 3 * *"

jobs:
  build-site-data:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install system packages
        run: |
          sudo apt-get update
          sudo apt-get install -y gdal-bin libgdal-dev

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run pipeline
        env:
          BLACKMARBLE_TOKEN: ${{ secrets.BLACKMARBLE_TOKEN }}
          DATA_START: 2024-01-01
          DATA_END: 2026-02-01
<<<<<<< HEAD
          REGIONS_FILE: data/regions/us_test_regions.geojson
=======
>>>>>>> df987095b5a2132def2a9b63e9155cef00b4b901
        run: |
          python scripts/fetch_blackmarble_monthly.py
          python scripts/build_metrics.py
          python scripts/export_site_data.py
          python scripts/make_social_card.py

      - name: Commit updated outputs
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/derived docs/data outputs/social
          git diff --staged --quiet || git commit -m "Update econ light data"
          git push
